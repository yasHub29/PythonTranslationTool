# config/settings.py
# アプリケーション設定（画像翻訳なし）

import os
import shutil

# ---------------------------------------------
# 対応するファイルタイプ
# ---------------------------------------------
# 画像翻訳機能を削除したため、PPTX と Excel のみをサポート
# DOCX を追加してフロントエンドと整合させます
SUPPORTED_TYPES = {
    '.pptx': 'pptx',
    '.xlsx': 'excel',
    '.docx': 'docx'
}

# ---------------------------------------------
# 出力ディレクトリ
# ---------------------------------------------
OUTPUT_DIR = os.path.join(os.getcwd(), 'data', 'output')
os.makedirs(OUTPUT_DIR, exist_ok=True)

# ---------------------------------------------
# Tesseract OCR の設定は不要のため削除
# ---------------------------------------------
# 画像翻訳を行わないため、Tesseract のパス設定を削除しました。

# ---------------------------------------------
# Google 翻訳設定
# ---------------------------------------------
# googletrans / deep_translator 用の安定エンドポイント
GOOGLETRANS_SERVICE_URL = ['translate.googleapis.com']

# ---------------------------------------------
# OCR 言語設定は不要のため削除
# ---------------------------------------------
# OCR_LANG = 'eng+jpn' は削除しました。
